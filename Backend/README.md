# Backend/

The always-on HTTP LeaderboardAdmin service was replaced by a **local maintenance tool**:

→ [`Tools/LeaderboardMaintenance/`](../Tools/LeaderboardMaintenance/)

Run that exe manually or via Task Scheduler. The game resolves workshop LB eligibility via Steam UGC ranking (no server).
