# Prompt: Implement Benchmark

Creá suite de benchmark CLI según `docs/Framework/08_DeveloperMode`.

- `dotnet run -- --benchmark <name>`
- Headless o mínimo UI
- Asserts pass/fail + exit code
- Gated o documentado para CI
- No depender de Steam para la suite core

Elegí escenarios del dominio del juego (física, pathfinding, spawn stress).
