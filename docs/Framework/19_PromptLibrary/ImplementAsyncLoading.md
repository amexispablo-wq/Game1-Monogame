# Prompt: Implement Async Loading

Pantalla/flujo de carga async según `docs/Framework/06_AssetPipeline`.

- Progress feedback
- Cancel al cambiar de escena
- Errores → log + mensaje UI
- No bloquear input loop sin feedback
- Compatible con Content Manager / IO suelto

Agnóstico al género (nivel, mundo, character).
